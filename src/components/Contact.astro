---
import { siteConfig as defaultConfig } from '../config.ts';
import { Icon } from 'astro-icon/components';

const { config = defaultConfig } = Astro.props;
---

<section id="contact" class="bg-mono-lighter px-4 py-20 sm:px-6 lg:px-8">
  <div class="mx-auto max-w-7xl">
    <div class="grid grid-cols-1 items-start gap-12 lg:grid-cols-2">
      <div class="scroll-fade-left">
        <h2 class="text-mono-heading mb-6 text-2xl font-bold sm:text-3xl lg:text-4xl">
          {config.contact.title}
        </h2>
        <p class="text-mono-dark mb-8 text-base leading-relaxed sm:text-lg">
          {config.contact.subtitle}
        </p>

        <ul class="mb-8 space-y-4">
          {
            config.contact.benefits.map((benefit) => (
              <li class="flex items-center">
                <Icon
                  name="heroicons:check"
                  class="text-mono-black mr-4 h-6 w-6 flex-shrink-0"
                />
                <span class="text-mono-dark">{benefit}</span>
              </li>
            ))
          }
        </ul>

        <div class="border-mono-light border-t pt-8">
          <p class="text-mono-gray mb-4 text-sm">
            Questions? Reach out to our team:
          </p>
          <a
            href={`mailto:${config.contact.email}`}
            class="text-mono-black font-semibold hover:underline">{config.contact.email}</a
          >
          {config.contact.phone && (
            <div class="mt-3">
              <a
                href={`tel:${config.contact.phone}`}
                class="text-mono-black font-semibold hover:underline">{config.contact.phone}</a
              >
            </div>
          )}
        </div>
      </div>

      <div
        class="scroll-fade-right border-mono-lighter rounded-xl border bg-white p-8 shadow-lg"
      >
        <form id="contactForm" class="space-y-6">
          <div>
            <label
              for="name"
              class="text-mono-heading mb-2 block text-sm font-semibold"
              >{config.contact.form.fields.name.label}</label
            >
            <input
              type="text"
              id="name"
              name="name"
              required={config.contact.form.fields.name.required}
              class="border-mono-lighter w-full rounded-lg border px-4 py-3"
              placeholder={config.contact.form.fields.name.placeholder}
            />
          </div>

          <div>
            <label
              for="email"
              class="text-mono-heading mb-2 block text-sm font-semibold"
              >{config.contact.form.fields.email.label}</label
            >
            <input
              type="email"
              id="email"
              name="email"
              required={config.contact.form.fields.email.required}
              class="border-mono-lighter w-full rounded-lg border px-4 py-3"
              placeholder={config.contact.form.fields.email.placeholder}
            />
          </div>

          <div>
            <label
              for="company"
              class="text-mono-heading mb-2 block text-sm font-semibold"
              >{config.contact.form.fields.company.label}</label
            >
            <input
              type="text"
              id="company"
              name="company"
              class="border-mono-lighter w-full rounded-lg border px-4 py-3"
              placeholder={config.contact.form.fields.company.placeholder}
            />
          </div>

          <div>
            <label
              for="message"
              class="text-mono-heading mb-2 block text-sm font-semibold"
              >{config.contact.form.fields.message.label}</label
            >
            <textarea
              id="message"
              name="message"
              rows="4"
              class="form-input border-mono-lighter w-full resize-none rounded-lg border px-4 py-3"
              placeholder={config.contact.form.fields.message.placeholder}
            ></textarea>
          </div>

          <button
            type="submit"
            class="bg-mono-black w-full rounded-lg px-6 py-4 font-semibold text-white shadow-lg"
          >
            {config.contact.form.cta}
          </button>

          <p class="text-mono-gray text-center text-xs leading-relaxed">
            By submitting this form, you agree to our
            <a href="#" class="underline">Terms of Service</a> and
            <a href="#" class="underline">Privacy Policy</a>
          </p>
        </form>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const contactForm = document.getElementById('contactForm');
    if (contactForm) {
      contactForm.addEventListener('submit', (e) => {
        e.preventDefault();
        alert('Thank you for your message! We will get back to you soon.');
        contactForm.reset();
      });
    }
  });
</script>
