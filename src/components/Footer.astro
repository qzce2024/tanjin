---
import { siteConfig as defaultConfig } from '../config.ts';
import { Icon } from 'astro-icon/components';

const { config = defaultConfig } = Astro.props;

const iconMap = {
  twitter: 'simple-icons:x',
  github: 'simple-icons:github',
  dribbble: 'simple-icons:dribbble',
  linkedin: 'simple-icons:linkedin',
};
---

<footer class="bg-mono-black px-4 py-16 text-white sm:px-6 lg:px-8">
  <div class="mx-auto max-w-7xl">
    <div class="mb-12 grid grid-cols-1 gap-12 md:grid-cols-4">
      <div class="md:col-span-1">
        <div class="mb-4 text-2xl font-bold">{config.site.name}</div>
        <p class="text-white/70 text-sm leading-relaxed">
          {config.footer.tagline}
        </p>
      </div>

      {
        config.footer.columns.map((column) => (
          <div>
            <h4 class="mb-4 font-semibold">{column.title}</h4>
            <ul class="space-y-3 text-sm">
              {column.links.map((link) => (
                <li>
                  <a href={link.href} class="text-white/70 hover:text-white transition-colors duration-200">
                    {link.name}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>

    <div
      class="border-white/20 flex flex-col items-center justify-between border-t pt-8 md:flex-row"
    >
      <p class="text-white/60 mb-4 text-sm md:mb-0">
        &copy; {config.footer.copyright}
      </p>

      <div class="flex space-x-6">
        {
          config.footer.social.map((social) => (
            <a
              href={social.href}
              class="text-white/70 hover:text-white transition-colors duration-200"
              aria-label={social.name}
            >
              <Icon name={iconMap[social.icon]} class="h-6 w-6" />
            </a>
          ))
        }
      </div>
    </div>
  </div>
</footer>
